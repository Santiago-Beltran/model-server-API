# model-server-API
REST API with a predict endpoint and rate limiting that allows you to quickly adapt to your specific model and deploy easily without reinventing the wheel.

It includes:
- Global rate limitting
- API Key config
- Three configs: Development, Testing, and Production.
- Rotating file logging set up in JSON format, as well as console logging using Rich.
